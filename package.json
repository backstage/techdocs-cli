{
  "name": "@techdocs/monorepo",
  "description": "See packages/techdocs-cli",
  "version": "0.0.0",
  "private": true,
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/backstage/techdocs-cli",
    "directory": "."
  },
  "scripts": {
    "build": "./build.sh",
    "test": "yarn workspace @techdocs/cli run test",
    "tsc": "yarn workspaces run tsc",
    "start": "yarn workspace @techdocs/cli run start"
  },
  "keywords": [
    "backstage",
    "techdocs"
  ],
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@backstage/cli": "^0.3.1"
  }
}
